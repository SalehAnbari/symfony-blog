{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_post_edit' %}

{% block body %}
    <h1>Edit Post</h1>
    {{ include('admin/dashboard/form.html.twig', {
        form: form,
        include_back_to_home_link: true,
    }, with_context = false) }}

    <div class="mt-2">
        {{ include('admin/dashboard/delete_form.html.twig', {post: post}, with_context = false) }}
    </div>
{% endblock %}
